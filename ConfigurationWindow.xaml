<Window x:Class="CustomAltTab.ConfigurationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configurações - Custom Alt+Tab" Height="600" Width="700"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#FF2D2D30">
    
    <Window.Resources>
        <Style TargetType="GroupBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="10"/>
        </Style>
        
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="White"/>
        </Style>
        
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
        </Style>
        
        <Style TargetType="Button">
            <Setter Property="Background" Value="#FF0E639C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF1177BB"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Título -->
        <Border Grid.Row="0" Background="#FF252526" Padding="15">
            <TextBlock Text="⚙ Configurações do Custom Alt+Tab" 
                       FontSize="20" 
                       FontWeight="Bold" 
                       Foreground="White"/>
        </Border>
        
        <!-- Conteúdo -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Modo de Exibição -->
                <GroupBox Header="Modo de Exibição">
                    <StackPanel>
                        <Label Content="Escolha como deseja visualizar as janelas:"/>
                        <ComboBox Name="DisplayModeCombo" 
                                  SelectionChanged="DisplayModeCombo_SelectionChanged"
                                  Margin="0,5,0,0"
                                  Height="30"
                                  Background="#FF3F3F46"
                                  Foreground="White"
                                  BorderBrush="#FF007ACC">
                            <ComboBoxItem Content="🎯 Roda (estilo GTA 5)"/>
                            <ComboBoxItem Content="📱 Grid (grade de janelas)"/>
                        </ComboBox>
                    </StackPanel>
                </GroupBox>
                
                <!-- Configurações de Slots -->
                <GroupBox Header="Slots">
                    <StackPanel>
                        <Label Content="Quantidade mínima de slots:"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Slider Name="MinSlotsSlider" 
                                    Grid.Column="0"
                                    Minimum="4" 
                                    Maximum="16" 
                                    Value="8"
                                    TickFrequency="1"
                                    IsSnapToTickEnabled="True"
                                    ValueChanged="MinSlotsSlider_ValueChanged"
                                    Margin="0,5,10,0"/>
                            <TextBlock Name="MinSlotsText" 
                                       Grid.Column="1"
                                       Text="8" 
                                       Foreground="White" 
                                       FontSize="16" 
                                       FontWeight="Bold"
                                       VerticalAlignment="Center"
                                       Width="30"
                                       TextAlignment="Center"/>
                        </Grid>
                        
                        <CheckBox Name="ShowEmptySlotsCheck" 
                                  Content="Mostrar slots vazios" 
                                  Margin="0,15,0,5"
                                  Checked="ShowEmptySlotsCheck_Changed"
                                  Unchecked="ShowEmptySlotsCheck_Changed"/>
                        
                        <CheckBox Name="ShowUnconfiguredCheck" 
                                  Content="Mostrar janelas não configuradas" 
                                  Margin="0,5,0,0"
                                  Checked="ShowUnconfiguredCheck_Changed"
                                  Unchecked="ShowUnconfiguredCheck_Changed"/>
                    </StackPanel>
                </GroupBox>
                
                <!-- Gerenciamento de Slots -->
                <GroupBox Header="Gerenciar Posições de Aplicativos">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <ListBox Name="SlotsListBox" 
                                 Grid.Row="0"
                                 Height="200"
                                 Background="#FF3F3F46"
                                 Foreground="White"
                                 BorderBrush="#FF007ACC"/>
                        
                        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                            <Button Content="➕ Adicionar Slot" Click="AddSlot_Click"/>
                            <Button Content="✏ Editar Slot" Click="EditSlot_Click"/>
                            <Button Content="🗑 Remover Slot" Click="RemoveSlot_Click"/>
                        </StackPanel>
                    </Grid>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>
        
        <!-- Botões de Ação -->
        <Border Grid.Row="2" Background="#FF252526" Padding="15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="💾 Salvar" Click="Save_Click" Width="120"/>
                <Button Content="✖ Cancelar" Click="Cancel_Click" Width="120"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>

<Window x:Class="CustomAltTab.SlotEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Editar Slot" Height="300" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#FF2D2D30">
    
    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="White"/>
        </Style>
        
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#FF3F3F46"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF007ACC"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Height" Value="30"/>
        </Style>
        
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#FF3F3F46"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF007ACC"/>
            <Setter Property="Height" Value="30"/>
        </Style>
        
        <Style TargetType="Button">
            <Setter Property="Background" Value="#FF0E639C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF1177BB"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Título -->
        <Border Grid.Row="0" Background="#FF252526" Padding="15">
            <TextBlock Name="SlotNumberText" 
                       Text="Configurar Slot" 
                       FontSize="18" 
                       FontWeight="Bold" 
                       Foreground="White"/>
        </Border>
        
        <!-- Conteúdo -->
        <StackPanel Grid.Row="1" Margin="20">
            <Label Content="Tipo do Slot:"/>
            <ComboBox Name="SlotTypeCombo" 
                      Margin="0,5,0,20"
                      SelectionChanged="SlotTypeCombo_SelectionChanged">
                <ComboBoxItem Content="📱 Aplicativo (janela específica)" IsSelected="True"/>
                <ComboBoxItem Content="━ Minimizar Atual (minimiza janela ativa)"/>
                <ComboBoxItem Content="✕ Cancelar (fecha o Alt+Tab)"/>
            </ComboBox>
            
            <Label Name="ExecutableLabel" Content="Nome do Executável (ex: Discord, Opera, Chrome):"/>
            <TextBox Name="ExecutableNameText" Margin="0,5,0,15"/>
            
            <Label Name="ProcessLabel" Content="Ou selecione de um processo em execução:"/>
            <ComboBox Name="ProcessCombo" 
                      Margin="0,5,0,15"
                      SelectionChanged="ProcessCombo_SelectionChanged"/>
            
            <TextBlock Name="TipText" 
                       Text="💡 Dica: O nome do executável é case-insensitive (Discord, discord ou DISCORD funcionam)." 
                       Foreground="#FFB0B0B0" 
                       TextWrapping="Wrap"
                       Margin="0,10,0,0"/>
        </StackPanel>
        
        <!-- Botões -->
        <Border Grid.Row="2" Background="#FF252526" Padding="15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="🗑 Limpar" Click="Clear_Click" Width="100" Background="#FF666666"/>
                <Button Content="💾 Salvar" Click="Save_Click" Width="100"/>
                <Button Content="✖ Cancelar" Click="Cancel_Click" Width="100"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
